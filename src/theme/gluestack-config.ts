import { createConfig } from '@gluestack-ui/themed';

// Create and export the config
export const config = createConfig({
  // You can customize the default theme here
  tokens: {
    // Define custom colors, spacing, etc. if needed
  }
});
